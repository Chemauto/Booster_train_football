"""Task packages for the booster_train/deploy sim2sim copy.

This file exists so `tasks` resolves here (sys.path[0]) instead of the
sibling booster_deploy/tasks package, which also has an __init__.py.
"""
