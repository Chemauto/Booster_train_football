# 当前数据与训练检查（14:35 后）

本报告更新之前的审计，不将旧版accad_markers的问题重复归到已重建文件上。用户要求以当前代码和数据为主，可以从零训练。检查中发现已经有从零训练，因此保留该次训练进度，仅在检查点暂停做独立评估。

## 当前有效版本

- 训练来源：`logs/rsl_rl/k1_kick_amp/2026-09-18_11-24-37`，启动参数checkpoint=null，2048环境，seed42，soccer默认随机化和虚拟感知。
- 实际数据：`amp_paper_waistfix`，24段5314帧；启动源码快照与当前source目录逐文件相同，已经包含球状态时序修复。
- accad_markers已在此前重建，79段22640帧；四元数和中性手臂的问题已修复，不再适用早上旧文件的179.84度/手臂大罚项结论。
- 干净退出保存`model_1445.pt`后进行当前策略评估；不是加载旧model_5800。

## 当前两套数据的独立复查

| 数据 | 硬角度/速度超限 | 非有限值 | FK最大误差 | walk软限位罚/步 | kick软限位罚/步 |
|---|---:|---:|---:|---:|---:|
| amp_paper_waistfix | 0 / 0 | 0 | 2.41e-7m | -0.02025 | -0.01723 |
| accad_markers | 0 / 0 | 0 | 2.83e-7m | -0.08187 | -0.20850 |

软限位罚是将专家保存姿态代入当前奖励计算的值，不是策略训练时的实际奖励。新markers数据并非所有方面优于论文数据：还有武术/跳跃/持物类别混在walk中，kick部分仍是武术踢；19.48%的kick帧摆动脚最低鞋底高于球顶0.22m。因此更直接的K1重定向路线值得保留，但不能因源机器人是K1就认定足球任务适配更好。

验收脚本已加强有限值、必需字段/形状、名字、方向性关节限位、精确速度、四元数及损坏文件报告。当前hard均为0并不证明动态可跟踪、无自碰撞或可实现力矩。

## 本轮落地修改

1. **数据验收的离地误报**：过去使用踝link原点超过5cm判断双脚腾空，正常站姿也会触发。现在复用训练模型的四个鞋底角点，结合每帧足部旋转判断最低鞋底高度。新增正常踝高/真实抬脚回归测试。
2. **IK模型与训练模型对齐**：`accad_markers_to_k1.align_ik_model`按关节名对齐关节/连杆变换和URDF硬限位，拒绝轴向不一致。GMR旧资产髋部位置差15mm、部分关节范围更宽，不能只在最终输出裁剪。修改后重新创建mink.ConfigurationLimit，确保约束在求解时生效。
3. **实际样本验证**：独立新目录生成B3 walk1，381帧，hard0，仅2项soft；同姿态IK模型与训练模型足端FK一致。这个结果只验证一段动作，**未覆盖现有accad_markers目录，也未声称已完成全套重建或动态验收**。
4. **转换记录纠错**：删除convert_paper_data里已经被否定的44.8度、23%不可达及精确保姿态说法；腰yaw加到hip yaw是不同转轴链上的近似，旋转不交换。未来输出metadata改为真实映射并记录源、模型、生成器哈希。没有改动当前训练NPZ里的角度或速度。
5. **分段训练保留原训练设置**：现有run_kick_amp_stages原先强制nominal_physics/perfect_perception/near_ball，不能直接拿来继续当前全随机化实验。增加`--training_profile default`保留默认训练设置、`--motion_dir`固定数据及哈希检查；每阶段结束独立评估。源码/数据改变则停止后续阶段，避免混合实验。

## 当前策略的独立评估

`model_1445.pt`，64环境，30秒固定首轮队列，seed123，均值动作，虚拟感知保留；评估物理为nominal、10ms固定电机延迟，无随机推搡/踢球/传送。

- 存活至30秒：16/64（25%）。
- 摔倒：48/64（75%），已观测摔倒平均时刻2.15秒。
- 触球代理、验证进球：均0。
- 无球位置跳变；不是把复位位移当成移动/进球。

所以当前策略尚未学稳，更未实现自主踢球。最近100条训练记录的平均朝球速度仅0.00293m/s；平面速度0.187m/s不能等同于有效接近。损失数值有限，不构成成功证据。

在此检查点继续每500次更新自动评估，保留原网络/优化器和全随机化配置，不重用旧训练模型，也不因单次早期评估失败任意修改奖励。阶段输出会记录实际物理指标，后续依据这些结果判断延长或进一步诊断。

## 验证和证据

73项CPU测试及16个子测试通过，包含损坏数据、identity约定、模型FK、球状态/奖励时序、训练进程编排。`git diff --check`通过。证据目录：`logs/audit_current_20260918_1435/`，包含数据全量数值报告、门禁、生成样本、检查点评估、训练指标及暂停/续训记录。

剩余已知项仍需受控实验：执行器43/76Nm配置不一致、非对称nominal的镜像语义、地形/扰动与数据动态可实现性。本轮未通过增大力矩或删除惩罚来掩盖它们。

真实Isaac4环境修复复验：第50个进球帧的任务奖励为+0.3（四环境一致），成功标记均为1，球重置后perfect真实观察误差均为0。证据`runtime_fixed.json`。

已启动分段续训：从本次scratch模型1445继续，2048环境、seed42、default训练配置，显式固定amp_paper_waistfix，每500次评估一次，首批4阶段；不重置优化器/critic。控制记录`logs/audit_current_20260918_1435/stages/status.json`，首阶段目标1945。完整自主踢球目标仍未达到。

## 后续状态复查：1945与2000检查点

同协议64环境、30秒、seed123：1945为48满程、7摔倒、9出界、46触球代理、0进球；2000为41满程、3摔倒、20出界、55触球代理、1进球。两轮均无球位置跳变。2000平均稳定左右支撑切换45.28次、平均净位移4.71m。已出现明显行走/接近/触球进展，但定向射门和边界控制不足，不能视为完成。

第二阶段原目标2445，实际在2000后的更新发生CUDA OOM，申请186MiB时只剩169.5MiB；堆栈位于runner.py的mirrored_stack_b生成。该处已经持有stacked_b，却又从buf重新索引同一历史batch，会额外复制约186MiB。控制器随后评估保存的2000并继续第三阶段，因此第二阶段并未完成500更新。后续需复用stacked_b减少无效复制，并让控制器核实实际完成的迭代数、将OOM/异常退出记录为中断；不能只信子进程返回码。此次状态复查未在正在运行的实验中修改训练源码，第三阶段从2000继续至2500。
