# K1 当前实现与动作先验重建

用户已授权按审计发现修复、以新K1数据为主评估并从零训练；旧模型仅历史参考。目标仍为通过球/球门位置自主行走到球并将球踢入门，不以站立或少数偶发进球宣称完成。

采用直接ACCAD标记→K1 IK路线，纠正wxyz identity和经证据确认的模型坐标问题；在新版本目录重建，保留源数据与生成代码哈希。相较继续用T1直接角度搬运，直接K1 IK可以处理形态，但必须验证目标和物理约束。拒绝仅裁剪到硬限位就宣布动态可行。

统一奖励物理转移与后续观察的时序。瞬时塑形读取当前仿真状态；球重置之前计算该物理转移奖励，之后同步球状态、真实观察和感知采样。默认感知延迟保留。

验收：必需字段、形状、有限值、模型关节角/速度、四元数和FK一致；另量化软限位奖励、接触滑移、朝向、步态与足球踢法覆盖。校正后的少量片段先做运动学及Isaac运行时检查，再完整生成并启用。不要靠全面增大电机力矩补数据错误。

保持网络/AMP/PPO主结构、有效GP50，避免同时改无因果证据的超参。修复当前明确缺陷后保存完整配置，scratch短训并做固定首轮队列评估。检查通过才能延长；失败依据日志与真实状态诊断。

单张8GB GPU只运行一个Isaac进程；CPU数据构建可并行。保留当前脏工作树其他修改，不批量还原或提交。
