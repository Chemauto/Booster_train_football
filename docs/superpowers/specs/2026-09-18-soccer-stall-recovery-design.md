# K1 Soccer Stall Recovery

The user requests continued fixes and training toward autonomous scoring. Iterations 2000/2500/3000 have contact proxies 55/15/1 of 64; stable first-episode survival rises to 64/64. A real-physics probe of model_3000 confirms final median trunk-ball distance 0.614 m, last 10 s mean planar speed 0.0092 m/s, and all sampled final 10 s states stationary (<0.1 m/s) within 1 m. Ball is effectively motionless. This is a near-ball stopping strategy, not a standing failure. Evidence is logs/audit_soccer_stall_20260918/model3000_probe.json and probe_summary.json.

Current curriculum multiplies normalized goal-critic advantages by iteration/16000 (0.1875 at iteration 3000), on top of its configured coefficient 2. The local reference code uses the full 2:1 goal/auxiliary advantage mixture. The imported stagnation reward exempts a robot within 1 m. Gait shaping rewards stop at 0.5 m. These facts support the hypothesis that task weighting is insufficient to learn the last approach and ball manipulation, but do not by themselves prove a single cause.

Alternatives considered: immediately shrink stagnation exemption/change toe penalty (multiple behavior-specific changes, may encourage unsafe rushing), replace motion dataset (current behavior already demonstrates movement and current dataset has no hard validation findings), or test independent soccer-task weighting. Choose the last first: add task_weight_floor with default 0 and experimental value 1, applied only to normalized task-policy advantages. Preserve gait/stagnation schedules, reward functions, safety costs, dynamics, observations and motion data. Do not reset critics because raw reward targets are unchanged.

Run matched bounded continuations from model_3000 with identical seed, 1024 environments and 200 updates, sequentially on the single GPU. Baseline uses floor 0; treatment uses floor 1. Fixed 64-scene, 30 s evaluations retain existing mean-action/no-intervention protocol. Track falls, boundary exits, contact proxy, directed ball progress and valid goals. Promote only with physical evidence; if no improvement, revise the hypothesis instead of multiplying changes. The full user goal is not considered complete at this checkpoint.

Independently repair verified infrastructure faults: reuse already selected history for symmetry, and require explicit full-budget training completion before controller evaluation/continuation. A partial checkpoint after zero-exit OOM is not a completed stage. Default behavior and graceful checkpoint preservation remain supported.
