# 独立接近球阶段

用户已授权直接修改并训练。旧联合课程的稳定样本仍仅移动约6cm，停止旧调度并保存。

- 修复停滞阈值：目标0.4m/s不能因过去1秒位移不足0.7m被罚；阈值改0.1m。
- 新增显式approach阶段：冻结课程，不按迭代减弱行走奖励；关闭踢法/球门项；行走数据；默认站姿重置。
- 受控物理与准确观测；速度目标0.4m/s，跟踪权重8；存活1，终止-100；AMP0.3。该组是待验证实验配置。
- 奖励目标改变后保留actor/encoder，重置critic和PPO优化器；恢复探索标准差0.15。后续同阶段恢复不重置。
- 固定approach评估：64个场景、多朝向、球前方1–2m。首次回合持续30秒，不重置计分。
- 成功同时要求未跌倒、净位移>=0.5m、距离球曾<=0.55m、至少4次经60ms稳定确认的左右单脚支撑切换。
- 阶段门槛：存活>=90%、成功>=80%，两个seed均满足；未通过继续同阶段，绝不按迭代宣称升级。
- CPU回归、实际GPU运行及分阶段训练/评估。保留旧检查点，记录变化，不承诺新奖励一定成功。
