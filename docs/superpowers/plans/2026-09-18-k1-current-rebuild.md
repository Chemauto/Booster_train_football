# K1 重建执行计划

设计：../specs/2026-09-18-k1-current-rebuild-design.md；用户已经授权执行，不需再等待批准。

1. [已复现] 当前新数据180°目标偏移、软限位冲突；球重置缓存和逐步奖励时序；验收漏检。
2. [进行中] 独立任务：球状态修复及回归测试；数据验收修复及损坏输入测试；重定向其他坐标/形态复核。
3. [进行中] 为标记IK增加identity/朝向回归，修复wxyz并生成小规模新样本，量化软限位、FK、接触和朝向。必要时修复已验证的足端映射问题。
4. 在版本化新目录完整构建经筛选的K1数据，保存完整清单、源/模型/配置/生成器哈希及验收报告；校正并限定reset与expert的手臂语义。
5. 集成审查与全部相关CPU测试；真实Isaac边界复现及少量动作复位/观测检查，不与训练抢GPU。
6. 仅启用验收后数据，保存从零训练的有效配置和启动命令；短训，检查finite/梯度/回合/真实移动，固定队列独立评估，然后据结果延长或诊断。
7. 报告已修复、数据验收结果、实际训练状态和仍未达到的任务指标。完整自主踢球未达标前不能标完成。

## 14:35 后的状态调整

继续时工作树/数据已由另一轮工作更新，且11:24 scratch训练已经运行。因此重新验收当前两套数据并保留实际amp_paper_waistfix训练；不覆盖其数据或重启为另一先验。球时序与数据门禁已修，73项测试通过；IK模型对齐仅新单段样本验收通过，全量新IK数据重建仍未完成。1445检查点独立评估仅25%存活、0触球/进球，已从该新训练检查点启动default配置的4×500更新与自动评估。后续优先读取stages/status.json再决定下一步，不重复使用早上已失效的旧accad数值。
