# K1 自主足球训练修复与验证计划

目标：保留论文的 AMP + PPO + 历史观测结构，修复可复现的移植错误，训练并以独立物理指标验证根据球和球门位置移动、踢球。用户已授权自主修改、训练、评估及迭代；不另行请求常规修复确认。

当前证据与取舍：
- 采用现有 AMP 路线，先修移植契约，再判断数据质量。单纯加大奖励/延长训练不能纠正坐标或传感器索引。
- 暂不退回固定时间轴踢球模仿；它无法直接满足自由接近目标的需求。
- 若 AMP 在正确数据流下仍不产生运动，先隔离运动数据/执行器的可实现性，再考虑分阶段运动控制，不宣称算法本身无效。

- [x] 留存原始源代码、修改、日志、训练命令和 cron；用原 monitor 文件锁排除旧自动改码程序。
- [x] 核对论文的方法、参考 T1 实现、本地 K1 移植、TensorBoard 回合指标。
- [x] 在实际 Isaac Lab 中核对 robot.body_names 与 sensor.body_names，验证默认动作连续存活。
- [ ] 为偏航角、接触语义、归一化镜像、时间对齐、导出副作用建立 CPU 回归测试，先运行失败。
- [ ] 修复 wxyz 偏航角、传感器名称索引、当前状态重建目标和对称归一化，保持所有更改可追踪。
- [ ] 修复 --play、确定性评估、模型保存/退出和日志指标，固定随机种子。
- [ ] 审核动作数据：落地高度、关节范围、速度一致性和脚底滑动。保留原始文件，不用降速掩盖不一致。
- [ ] 单 GPU 短训检查有限值、正常梯度及 checkpoint 可恢复；随后启动独立命名训练。
- [ ] 定期用无重置连续存活、位移、接近距离、真实踢球/进球率评价；不以单步未摔比例替代成功率。
- [ ] 按证据继续迭代，记录未完成项；只有真实完成自主移动和进球才标记总目标完成。

验证命令：`/home/xcj/miniconda3/envs/env_isaaclab/bin/python -m unittest discover -s tests -v`，Isaac Lab headless 物理审计及阶段性独立评估。已有训练约 65 个运行目录；不删除旧结果、不提交混杂的用户改动。
